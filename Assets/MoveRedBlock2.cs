using UnityEngine;

public class MoveRedBlock2 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //move red block 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
